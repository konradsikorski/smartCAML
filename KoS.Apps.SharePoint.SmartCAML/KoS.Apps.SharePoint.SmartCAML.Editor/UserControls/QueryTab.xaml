<UserControl x:Class="KoS.Apps.SharePoint.SmartCAML.Editor.Controls.QueryTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KoS.Apps.SharePoint.SmartCAML.Editor.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="60*"/>
        </Grid.RowDefinitions>
        
        <TabControl Grid.Row="0">
            <TabItem Header="Designer" x:Name="DesignerTab" >
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <local:QueryBuilderControl x:Name="ucQueryBuilder" Margin="4" />
                </ScrollViewer>
            </TabItem>
            <TabItem Header="XML" x:Name="XmlTab">
                <TextBox x:Name="ucQuery" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" />
            </TabItem>
        </TabControl>

        <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch" Cursor="SizeNS" ResizeDirection="Rows" />

        <local:ItemsGrid  Grid.Row="2" x:Name="ucItems" />
    </Grid>
</UserControl>
