<UserControl x:Class="KoS.Apps.SharePoint.SmartCAML.Editor.Controls.QueryFilterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KoS.Apps.SharePoint.SmartCAML.Editor.Controls"
             xmlns:componentModel="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300"
             Padding="2">
    <UserControl.Style>
        <Style TargetType="{x:Type UserControl}">
            <Setter Property="Background" Value="Transparent" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFFDFDDE" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>

    <UserControl.Resources>
        <CollectionViewSource Source="{Binding Path=Fields}" x:Key="Fields">
            <CollectionViewSource.SortDescriptions>
                <componentModel:SortDescription PropertyName="InternalName" />
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>

        <Style TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="#FF6F6F89"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#FF5492EC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <StackPanel x:Name="ucContainer" Orientation="Horizontal">
        <Button Content="✖"  Margin="4,0,0,0" Click="RemoveFilterButton_Click" Background="Transparent" Padding="4, 0" BorderBrush="Transparent" />
        <Button Content="▲" Click="UpButton_OnClick" Background="Transparent" BorderBrush="Transparent" Padding="4, 0" />
        <Button Content="▼" Click="DownButton_OnClick" Background="Transparent" BorderBrush="Transparent" Padding="4, 0" />
        <ComboBox x:Name="ucAndOr" Margin="4,0,0,0"/>
        <ComboBox x:Name="ucField" Margin="4,0,0,0" IsEditable="True" ToolTip="Field" ItemsSource="{Binding Source={StaticResource Fields}}" DisplayMemberPath="InternalName" SelectionChanged="ucField_OnSelectionChanged" />
    </StackPanel>

</UserControl>
